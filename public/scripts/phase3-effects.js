window.Phase3Effects=(function(){function showBanner(text){const el=document.getElementById('alert-banner');if(!el)return;el.textContent=text;el.classList.add('visible');clearTimeout(window.__bannerTimer);window.__bannerTimer=setTimeout(()=>el.classList.remove('visible'),3500);}function showAchievement(text){const el=document.getElementById('achievement-toast');if(!el)return;el.textContent=text;el.classList.add('visible');clearTimeout(window.__achievementTimer);window.__achievementTimer=setTimeout(()=>el.classList.remove('visible'),3500);}function showHint(x,y,title,summary){const el=document.getElementById('hotspot-hint');if(!el)return;el.innerHTML=`<strong>${title}</strong><div style="margin-top:6px;color:#bbb">${summary}</div>`;el.style.left=`${x}px`;el.style.top=`${y}px`;el.style.display='block';}function hideHint(){const el=document.getElementById('hotspot-hint');if(el)el.style.display='none';}return{showBanner,showAchievement,showHint,hideHint};})();