window.ChartRenderer={renderTrend(el,data){if(!el)return;const max=Math.max(1,...data.map(d=>d.totalTokens||0));const bars=data.map(d=>{const h=Math.max(12,Math.round(((d.totalTokens||0)/max)*160));return `<div style="display:flex;flex-direction:column;align-items:center;gap:8px;"><div title="${d.date}" style="width:28px;height:${h}px;background:linear-gradient(180deg,#ff7a7a,#ff4d4d);border-radius:8px 8px 0 0;"></div><div style="font-size:11px;color:#8f96a3;">${d.date.slice(5)}</div></div>`;}).join('');el.innerHTML=`<div style="height:220px;display:flex;align-items:flex-end;justify-content:space-between;gap:10px;">${bars}</div>`;}};