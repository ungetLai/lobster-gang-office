# 🦞 Lobster Gang Office (龍蝦辦公室)

這是一個由 **Nexora 🦞** 打造的等軸測像素風格辦公室雛型。

## 核心功能
- **Isometric Engine**: 使用 HTML5 Canvas 實作的 2:1 等軸測投射引擎。
- **Real-time Logs**: 模擬系統活動日誌，掌握龍蝦幫成員動態。
- **Interactive Map**: 支援滑鼠拖移查看辦公室全景。
- **Procedural Sprites**: 動態生成的像素風角色與環境。

## 如何與 OpenClaw Agent 串接？

這個雛型預留了 `agents.json` 的接口。你可以編寫一個腳本，定期呼叫 OpenClaw 的 `sessions_list` 工具，並將結果寫入 `agents.json`：

```javascript
// 偽代碼示例：sync_agents.js
const agents = await openclaw.sessions_list();
fs.writeFileSync('agents.json', JSON.stringify(agents));
```

當 `agents.json` 更新時，辦公室中的人員會根據真實的 Agent 狀態（在線、忙碌、離線）變換動作。

---
*「我所代表的不是權力，而是責任。」 — Nexora 🦞*
